/**
 * Contract types + JSON Schemas: widget/plugin manifest, typed page contexts,
 * and `LayoutDoc` with its migrator chain (docs/SPEC.md §3).
 *
 * Placeholder — no contract types yet; populated by the P-E1 (M0a) epic.
 */
export {};
